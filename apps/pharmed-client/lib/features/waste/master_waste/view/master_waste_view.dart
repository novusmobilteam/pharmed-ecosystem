import 'package:flutter/widgets.dart';

class MasterWasteView extends StatelessWidget {
  const MasterWasteView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
